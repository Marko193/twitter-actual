extends layouts/main-layout.pug

block content
    if !profileUser
        span.errorMessage Check the URL you`re trying to access!
    else
        span #{profileUser.firstName}
        .postsContainer

        +createPostModals(userLoggedIn)

block scripts
    script(src="/js/profile.js")